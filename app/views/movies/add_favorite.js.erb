<% if current_user.movies.pluck(:movie_id).include?(@movie["id"].to_s) %>
$("#favorite_link").html("<%= j link_to 'Remove Favourite', remove_favorite_movie_path(@movie), method: :delete, remote: true, class: 'start btn btn-primary' %>");
<% end %>
